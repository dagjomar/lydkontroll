# Browser and end-to-end tests

Standalone Playwright desktop and Mobile Safari coverage will be added with the
mobile-control slice. Native Tauri behavior is covered at the Rust boundary and
by manual macOS checks because macOS has no native Tauri WebDriver.
